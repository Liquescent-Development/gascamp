---
description: Show the campd fleet snapshot — live sessions, ready and open beads. Wraps the camp CLI.
allowed-tools: Bash(camp:*)
---
One status query rendered as plain text (auto-starts campd if it is down):

```!
camp top
```
